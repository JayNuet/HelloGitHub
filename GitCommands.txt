git checkout -b "NameOfNewBranch" -> this command allows the user to create a new branch from the master branch
git push -> allows the user to add local changes to the remote repository
git pull -> allows the  user to download commits that don't exist on their machine from a remote repository
git clone -> allows the user to clone a repository into a newly created directory